#include <iostream>

int main() {
  int length = 20; // literal integer
  int width = 40;
  int area =
      length * width; // variable named area looking for object called length
                      // and width to use the "*" expprestion to it

  std::cout << area << std::endl;
  return 0;
}
