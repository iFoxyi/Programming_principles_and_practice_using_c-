#include <iostream>
#include <vector>

int main() {
  std::vector<int> v = {4, 5, 8, 1, 3}; // this is how we make vector instance
  std::vector<double> vd(6);
  std::vector<std::string> vs(9);

  return 0;
}
