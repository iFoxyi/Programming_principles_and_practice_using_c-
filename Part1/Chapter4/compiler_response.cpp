#include <iostream>

int area(int length, int width) {
  int result = length * width;
  return result;
}
int main() {
  std::cout << area('5', 4);
  std::cout << area(2, 3)
  std::cout << area(4,5;

  return 0;
}
